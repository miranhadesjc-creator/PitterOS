{
    "name": "pitter-os",
    "version": "0.1.0",
    "description": "Pitter OS - Backend Kernel em Tauri/Rust",
    "author": "Jean Pitter",
    "scripts": {
        "dev": "tauri dev",
        "build": "tauri build",
        "build-linux": "tauri build --target x86_64-unknown-linux-gnu",
        "tauri": "tauri"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.0"
    },
    "dependencies": {
        "@tauri-apps/api": "^1.5.0"
    }
}